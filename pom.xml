<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0     http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <properties>
        <verdict.version>0.4.0</verdict.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <antlr4.visitor>true</antlr4.visitor>
        <antlr4.version>4.5.3</antlr4.version>
        <apache.commons.version>3.5</apache.commons.version>
        <junit.version>4.12</junit.version>
        <google.guava.version>19.0</google.guava.version>
        <log4j.version>1.2.17</log4j.version>
        <maven.java.version>1.7</maven.java.version>
        <maven.source-plugin.version>3.0.1</maven.source-plugin.version>
        <maven.compiler-plugin.version>3.7.0</maven.compiler-plugin.version>
        <maven.assembly-plugin.version>3.1.0</maven.assembly-plugin.version>
        <maven.failsafe.version>2.13</maven.failsafe.version>
        <maven.surefire.version>2.12.4</maven.surefire.version>
        <maven.javadoc.version>2.10.4</maven.javadoc.version>
        <skipTests>true</skipTests>
        <spark.core.version>1.6.0-cdh5.11.1</spark.core.version>
        <spark.sql.version>1.6.0-cdh5.11.1</spark.sql.version>
        <spark.hive.version>1.6.0-cdh5.11.1</spark.hive.version>
        <spark2.core.version>2.2.0.cloudera1</spark2.core.version>
        <spark2.sql.version>2.2.0.cloudera1</spark2.sql.version>
        <spark2.hive.version>2.2.0.cloudera1</spark2.hive.version>
        <main.basedir>${project.basedir}</main.basedir>
    </properties>

    <groupId>edu.umich</groupId>
    <artifactId>verdict</artifactId>
    <packaging>pom</packaging>
    <version>0.4.0</version>
    <name>Verdict</name>
    <url>http://verdictdb.org</url>

    <modules>
        <module>core</module>
        <module>jdbc</module>
        <module>veeline</module>
    </modules>

    <repositories>
        <repository>
            <id>cloudera</id>
            <url>https://repository.cloudera.com/artifactory/cloudera-repos/</url>
        </repository>
        <repository>
            <id>umich-dbgroup</id>
            <url>http://salat2.eecs.umich.edu:9002/nexus/content/repositories/umich-dbgroup</url>
        </repository>
    </repositories>

    <build>
        <resources>
            <resource>
                <directory>${main.basedir}/conf</directory>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven.source-plugin.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler-plugin.version}</version>
                    <configuration>
                        <source>1.7</source>
                        <target>1.7</target>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>${maven.assembly-plugin.version}</version>
                    <executions>
                        <execution>
                            <phase>package</phase>
                            <goals>
                                <goal>single</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <descriptorRefs>
                            <descriptorRef>jar-with-dependencies</descriptorRef>
                        </descriptorRefs>
                        <appendAssemblyId>false</appendAssemblyId>
                        <outputDirectory>${main.basedir}/jars</outputDirectory>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.version}</version>
                    <configuration>
                        <skipTests>${skipTests}</skipTests>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>2.4.1</version>
                    <configuration>
                        <filesets>
                            <fileset>
                                <directory>${main.basedir}/jars</directory>
                                <followSymlinks>false</followSymlinks>
                            </fileset>
                        </filesets>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven.javadoc.version}</version>
                    <configuration>
                        <additionalparam>-Xdoclint:none</additionalparam>
                    </configuration>
                    <executions>
                        <execution>
                            <id>aggregate</id>
                            <goals>
                                <goal>aggregate</goal>
                            </goals>
                            <phase>site</phase>
                            <configuration>
                                <!-- Specific configuration for the aggregate 
                                    report -->
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
