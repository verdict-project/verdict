image: maven:3-jdk-7

stages:
  - build

services:
  - mysql:5.5
  - postgres:9.6

variables:
  BUILD_ENV: "GitLab"
  MYSQL_DATABASE: test
  MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
  POSTGRES_DB: test
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: ""

build:
  stage: build
  script: mvn package
